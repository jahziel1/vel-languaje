page Products {
    state {
        products = try api.get("https://fakestoreapi.com/products")
    }

    column(padding: 32) {
        match products {
            Loading       -> spinner()
            Success(data) -> list(data) { item -> text("item") }
            Error(msg)    -> text("Error: {msg}")
        }
    }
}
